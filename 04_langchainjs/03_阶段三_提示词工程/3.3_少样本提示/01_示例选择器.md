# 示例选择器（Example Selector）

## 基础用法

```javascript
import { PromptTemplate, FewShotPromptTemplate } from "@langchain/core/prompts";

const examples = [
  { input: "高兴", output: "positive" },
  { input: "难过", output: "negative" },
  { input: "平静", output: "neutral" },
];

const examplePrompt = PromptTemplate.fromTemplate(`
输入：{input}
输出：{output}
`);

const fewShotPrompt = new FewShotPromptTemplate({
  examples,
  examplePrompt,
  prefix: "判断情感倾向：",
  suffix: "输入：{input}\n输出：",
  exampleSeparator: "\n---\n",
});
```

---

## 核心流程图

```mermaid
flowchart TD
    subgraph InputLayer["输入层"]
        A[用户传入变量]
    end

    subgraph TemplateEngine["FewShotPromptTemplate 引擎"]
        B[验证 inputVariables]
        B -->|缺失变量| B1[抛出错误]
        B -->|验证通过| C

        C[拼接 Prompt 结构]

        subgraph StructureAssembly["结构组装"]
            C1[插入 prefix]
            C2[渲染 Few-Shot Examples]
            C3[插入 suffix]
        end

        C --> C1
        C --> C2
        C --> C3

        subgraph ExampleRendering["示例渲染流程"]
            C2 --> D1[遍历 examples 数组]
            D1 --> D2{每个示例}
            D2 --> D3[用 examplePrompt 模板格式化]
            D3 --> D4[示例1: 输入输出]
            D3 --> D5[示例2: 输入输出]
            D3 --> D6[示例N: 输入输出]
            D4 & D5 & D6 --> D7[用 exampleSeparator 连接]
        end

        C1 & D7 & C3 --> E[合并所有部分]
    end

    subgraph VariableReplacement["变量替换层"]
        E --> F[扫描最终模板]
        F --> G[匹配 inputVariables]
        G -->|变量存在| H[替换为实际值]
        G -->|变量不存在| G1[保留占位符或报错]
        H --> I[生成最终 Prompt 字符串]
    end

    subgraph OutputLayer["输出层"]
        I --> J[返回格式化后的 Prompt]
        J --> K[发送给 LLM 模型]
        K --> L[模型生成响应]
    end

    style InputLayer fill:#e3f2fd
    style TemplateEngine fill:#fff3e0
    style StructureAssembly fill:#fce4ec
    style ExampleRendering fill:#f3e5f5
    style VariableReplacement fill:#e8f5e9
    style OutputLayer fill:#ffebee
```

---

## 核心流程说明

| 阶段 | 功能 | 关键配置 |
|------|------|----------|
| **输入层** | 接收运行时变量 | `inputVariables` 定义预期变量 |
| **prefix** | 系统指令/任务定义 | 固定文本，无变量 |
| **examples** | 提供参考样本 | `examples` 数据 + `examplePrompt` 模板 |
| **suffix** | 放置用户实际问题 | 包含 `{input}` 占位符 |
| **变量替换** | 动态注入用户输入 | 匹配 `inputVariables` 与传入对象 |
| **输出层** | 生成最终 Prompt 给模型 | 完整上下文 + 问题 |
